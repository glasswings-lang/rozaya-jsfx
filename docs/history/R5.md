# R5 -- Names are sentence case throughout

**Done, and off the rules list, 2026-09-14.** Rozaya, 2026-09-14: *"Let's take them as one naming pass yeah. and then they get moved. I
don't care if something else refferences them. they're moving after they're done."* R2, R4
and R5 were done together, labels only: twelve plugins renamed, all twelve rendered
bit-identical to the commit before, `jsfx_map impact` found nothing else touched.

Checked after: every name is sentence case OUTSIDE its brackets. Inside brackets the text
was left as written, because notes there name real controls and options (`when Active is
off`, `Target default`, `in Semitones`). Acronyms, units and note names keep their capitals
(`HB`, `HRV`, `LFO`, `Hz`, `C#`, `V1`).

The rule as it stood, verbatim:

## R5. Names are sentence case throughout

`Start delay`, not `Start Delay`. `Drift mode`, not `Drift Mode`.

The suite currently uses Title Case for the transport block and sentence case for the
drift/ramp blocks, which is why `Start Delay` and `Speed ramp start delay` sit in the
same file. Sentence case is the newer convention and the larger block.

**Note honestly: this is a source-consistency fix, not an accessibility one.** NVDA does
not announce capitalisation. It matters for whoever reads the code, including us.
