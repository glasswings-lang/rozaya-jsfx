# R22 — history

**Budget: 200 lines**, like every file in `docs/history/`. Read this only to
find out whether something has already been tried and killed. The rule itself is
in `docs/suite-consistency-plan.md`.

## The argument Rozaya overturned, moved out of the plan 2026-09-09

## ~~Why `Note` is not one of the modes~~ — OVERTURNED 2026-09-09

**This section argued at length that `Note` could not be one of the pitch modes,
because a plugin that did not make the audio does not know its pitch. Rozaya
overturned it the first time she navigated the built block:** *"note is not
master here. it's one way of expressing pitch, period. Don't have two
finetunes."*

She was right, and the argument was a workaround wearing a rule's clothes. The
real constraint is that a JSFX slider can be a list of note names or a
continuous number, never both — which I could not express, so I promoted it to a
principle instead of saying it out loud. `docs/designing-for-dyscalculia.md` and
`CLAUDE.md` both already name that exact failure.

**Making `Note` the master also created a second fine tune.** Once the pitch
value is an OFFSET from a note, it stops being the pitch and becomes a fine
adjustment — so the block carried two of those, with the wrong half labelled as
coarse.

**The replacement** — mode first, a note name that is a real control in
Semitones mode and hidden elsewhere, the pitch value, then exactly one fine
tune — is in `docs/layouts/breath-gen.md`. Old text at commit `11b58ac`.


## The superseded reasoning, kept so it is not re-derived

Moved out of the plan 2026-09-09.

The first draft of this rule offered `{Note, Hz}` only, on the reasoning that a
heartbeat thump is a frequency and a drone voice is a note. **Rozaya killed
that, and was right to:** *"the minute I'm making decisions like this is the
minute I say include all of them."*

That draft was TRIAGE, which this suite's own rule forbids — a feature goes
everywhere its parent already is, and deciding per plugin which controls a
person may reach for is the same mistake in a different coat. It also had a
concrete cost I had not noticed: **`Pitch (semitones)` is how the Morpher,
Passage, Sustain Looper and Bubbler already state pitch.** A `{Note, Hz}` list
gives those four nowhere to land, so the "tidy" rule would have forced a unit
change on 122 Morpher instances plus Passage's, for nothing. Rozaya named the
three missing units in five words and the gap closed.

**Semitones and Cents are both offsets, and both are needed, for exactly the
reason `Every N beats` and `N per beat` are both needed.** They are the same
quantity at two scales. Keeping only semitones means seven cents is typed as
`0.07` — arithmetic, which is the barrier this suite exists to remove.

**What the offset modes are an offset FROM** is the plugin's own natural
reference: the captured sample's pitch in the Morpher and Passage, the loaded
loop in Sustain Looper, the base note elsewhere. That is what those controls
already mean; the mode only names it.


## Drafts of R22 that were written as though settled

Moved out of the plan 2026-09-09, with the attribution warning that heads this
rule. Each of these sat in the live rule reading like a decision:

- **`Note` cannot be one of the modes.** Overturned by Rozaya on 2026-09-09 the
  first time she navigated the built block. The full argument is above. As late
  as that same day the Morpher/Passage section still cited it as "the GENERAL
  rule ... and it stands", which is exactly the laundering CLAUDE.md warns about.
- **One complete pitch block per SOUND**, so Heartbeat would carry two and Womb
  four. Replaced by a single block behind a `Pitch target` selector, which is
  the suite's own existing pattern and a fixed cost per plugin.
- **"They carry the pair alone"** written into the rule statement for the
  Morpher and Passage. Rozaya said only that those two are their own discussion.
- **Deleting `Fine tune (cents)`**, on the reasoning that the pitch pair "does
  its job and more, and keeping both would be two ways to say one thing" -- and
  flagged at the time as the one reduction that came from correctness rather
  than tidying. It did not. Folding fine tune into the pitch pair means nudging
  a voice seven cents requires switching `Pitch mode` to Cents, which changes
  what the MAIN value means, nudging, and switching back. Rozaya, 2026-09-08:
  *"that's a long way from saying, we don't need this control because this other
  one, requiring more fiddling, looks tidier on paper."*
- **Leaving the `Pitch value` range open** as a question for Rozaya. R12 had
  already decided it in August.

## Womb was built and reverted, 2026-09-09 — an unexplained render difference

Four blocks replicated in place, one per frequency, Rozaya's call: *"womb is
3-layered. just replicate the block where it needs to be lol"*. 71 sliders -> 88,
one shared tuning reference with the master controls, and the whole thing
line-only so no blob had to be touched.

**Seven of the eight projects rendered bit-identical. `to-sleep-within` did not**
— 507,504 samples differing, first at 13.49 s. It is the only project with a
stepped drift running on a breath frequency.

What was ruled OUT before reverting, so nobody re-derives it:

- **The migration.** Every stored value was compared slot by slot, old line
  against new; nothing landed in the wrong place, and the four non-default
  frequencies (micle 650/370, womb-bubbles-proto 450/270, back-to-life's exhale
  200, to-sleep-within's S1 60) all moved correctly.
- **The renumbering.** Every `sliderN` reference in the code was mapped old to
  new and counted; all 71 matched.
- **The upper clamp.** `min(max(hz,20),20000)` versus the old `max(hz,20)`:
  reverting the clamp alone did not restore the render.
- **The resolver.** Bisected out entirely, putting the exact old arithmetic back
  in all six consumption sites. It STILL differed.
- **Reproducibility.** The same plugin twice on the same project is identical, so
  the difference is real, not run-to-run noise.

Two REAL bugs were found while chasing it, and both are worth remembering:

- **`womb_note_bind()` could never have worked.** eel2 passes parameters BY
  VALUE, so a function that assigns to `note` or `val` writes to a local copy and
  nothing reaches the slider. A note binding must be written inline.
- **`slider_automate` was being called on eight controls every @slider pass**,
  whether or not anything moved. Breath Generator only automates what actually
  changed.

**What is NOT ruled out:** that the difference is an artefact of the runner. Womb
renders as degenerate noise there — peak 6.95, RMS 4.3, which is not audio — and
`tools/jsfx_run/README.md` says filter frequency on a noise-based plugin is
invisible to it. A difference inside an unreliable measurement is not evidence
either way, which is exactly why this was reverted rather than shipped.

**To try again:** get a reliable measurement first. `kin_bridge.lua` can read
`eff_in_freq` out of the real plugin in REAPER, which the runner cannot.
