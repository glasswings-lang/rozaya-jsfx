# Heartbeat — eighteen Drift and Ramp targets, 2026-09-11

No slider moves; the two target lists grow and reorder. **Status: BUILT, MEASURED
2026-09-11. Not heard.** Snapshot `_pre-heartbeat-r24-20260911/`; previous build
in `jsfx-backups/effects-folder-baks/pre-heartbeat-r24-20260911/`.

## What Rozaya decided (quoted), and what is mine

- Heartbeat next: *"Then We can go for it."*
- Carrying over copies never migrated: *"Yes, sync all the broken things"*.
- R24 (Star): every sound-shaping control is a target, Play for and Rest for are,
  targets in control order.
- **Mine, unquoted:** the names; no "both" entries for the per-thump targets; new
  targets default to `On a clock` and step once per heartbeat on `With the
  target`; Stereo width never drifts across zero, because the sign picks the side
  and crossing it clears the delay line with a click; moving transformation's
  saved data to where REAPER writes it.

## The list — 4 become 18, in control order

0 Heart rate, 1 S1-S2 gap, **2 S1 volume, 3 S2 volume, 4 Brightness, 5 S1 decay,
6 S2 decay, 7 S1 pitch, 8 S2 pitch, 9 S1 fine tune, 10 S2 fine tune, 11 Tuning
reference, 12 Stereo width, 13 Breath cycle,** 14 Breath HRV depth, 15 Random HRV
depth, **16 Play for, 17 Rest for.** Old -> new: `0->0, 1->1, 2->14, 3->15`.

Pitch, fine tune, brightness, decays, width and breath cycle are rebuilt per block
from @slider's own formulas with drift and ramp folded in; volumes and play/rest
per sample. The thump pitches now derive in @block, where bank-derived values
belong.

## Save format and memory

Magic `2400004 -> 2500018`; 2400004 back to 2100004 are read at width four and
remapped by the plugin, selectors included, only what each held. Per-target banks
respaced to 32 slots from 8192.

## Instances, measured 2026-09-11

- Bridge copy: current, no file change.
- `finished/transformation.RPP`: its `<JS_SER>` sat INSIDE the `<JS>` block, where
  the 2026-09-09 pitch migration wrote it; REAPER writes it after. Moved, content
  unchanged. The only such block in any live project. Whether REAPER had been
  reading it is not known.
- Tensor's `transformation.RPP`: first release, 13 values; migrated by the rule
  Rozaya's copy got.
- Tensor's `tensor-heartbeat-pulse`: path unquoted with a space, so it never
  loaded; sixty typed zeros. Left untouched.

Tools: `tools/heartbeat_r24_migrate_20260911.py`, `tools/heartbeat_r24_verify_20260911.py`.
