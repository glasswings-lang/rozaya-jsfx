# Rozaya JSFX Plugin Suite — Manual

The manual is now **one page per plugin**, so you can read just the plugin you
installed instead of the whole suite at once.

➡ **Start here: [docs/plugins/](plugins/README.md)**

---

*This file used to hold the full combined manual. It was split into per-plugin
pages on 2026-07-08 so each plugin's docs travel with the plugin. Nothing was
lost — the split was a verbatim carve; see git history for the previous
single-file version.*
